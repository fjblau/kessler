# MongoDB Connection URI
# 
# Docker MongoDB (default - recommended for development):
# The Docker MongoDB instance runs on port 27018 to avoid conflicts 
# with any locally installed MongoDB on the default port 27017.
MONGO_URI=mongodb://localhost:27018
#
# Local MongoDB (alternative):
# If you prefer to use a locally installed MongoDB instance instead of Docker:
# MONGO_URI=mongodb://localhost:27017
#
# Remote MongoDB:
# For connecting to a remote MongoDB instance:
# MONGO_URI=mongodb://username:password@host:port/database
